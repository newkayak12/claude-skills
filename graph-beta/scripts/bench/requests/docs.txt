Produce the complete documentation set for this repository: a Node monorepo `tinyq` with three packages under packages/ (queue, retry, worker — each with its own package.json, src/index.mjs and tests) and no docs yet. Deliver: (1) packages/queue/README.md, packages/retry/README.md, packages/worker/README.md — each a reference for every export of that package with signatures, parameters, return values, thrown errors, and one runnable example each, verified against that package's code; (2) docs/architecture.md — package map, responsibilities, and the data flow from queue to worker to retry, derived from the actual code; (3) docs/adr/0001-bounded-queue.md, 0002-retry-policy.md, 0003-worker-concurrency.md — three architecture decision records reconstructing the design decisions evident in the code (context, decision, consequences, alternatives); (4) CONTRIBUTING.md — conventions inferred from the repository (package layout, test layout and runner, error class style, commit style) plus how to run tests; (5) root README.md — a short overview linking to all of the above with a 10-line quick start. Every document must be reviewed for accuracy against the source, not merely written; claims about behaviour that the code does not support must be removed or corrected before acceptance.
