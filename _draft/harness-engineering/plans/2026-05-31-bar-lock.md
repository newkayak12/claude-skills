# Bar-Lock (#006) Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사이클 시작 시 *품질 바*(gate 임계 / DoD / stage별 필수 리뷰)를 가설과 동일한 tamper-evident 해시 체인으로 잠가, "지친 에이전트가 중간에 바를 낮추는" 품질 저하 경로를 물리적으로 차단한다.

**Architecture:** 가설 체인 로직(`hypothesis-register.py`)을 공유 모듈 `chainlog.py`로 추출(중복 제거 + drift 방지) → 그 위에 `bar-register.py`(append-only `bar.jsonl`)를 형제로 추가 → 기존 PreToolUse hook `hypothesis-immutability.py`의 보호 대상을 `{hypotheses.jsonl, bar.jsonl}`로 확장 → `cycle-init.py`가 빈 `bar.jsonl`을 scaffold. 전체를 dev-tool dogfood 사이클로 감싸 자기적용(가설 등록 → 빌드 → self-test → retro → black box → 명시 종료).

**Tech Stack:** Python 3 (표준 라이브러리만), Claude Code hooks (PreToolUse), bash self-test (이 코드베이스의 기존 테스트 관용구 — pytest 없음).

**테스트 관용구 주의:** 이 코드베이스는 테스트 프레임워크가 없다. 기존 Sensor들(`hooks/README.md` Self-test 절)은 **bash 한 줄로 exit 코드/출력을 검증**한다. TDD의 "실패하는 테스트 먼저"를 이 관용구로 적용한다 — 각 작업은 (1) self-test 스니펫 작성 → (2) 실행해 실패 확인 → (3) 최소 구현 → (4) 통과 확인 → (5) 커밋.

**에이전트 스폰 주의:** writing-plans 표준의 `plan-document-reviewer` 서브에이전트 리뷰 루프는 이 저장소의 "사용자가 요청할 때만 에이전트 스폰" 지침에 따라 **생략**한다(사용자가 요청하면 추가). 대신 각 chunk 종료 시 self-review.

---

## File Structure

| 파일 | 상태 | 책임 |
|---|---|---|
| `plugin/harness/scripts/chainlog.py` | **신규** | 해시 체인 primitive (`compute_hash`/`last_hash`/`append_entry`/`verify_chain`). hypotheses·bar 공유 SSOT |
| `plugin/harness/scripts/hypothesis-register.py` | 수정 | 자체 체인 로직 → `chainlog` import로 교체 (동작 불변, 회귀 가드=기존 self-test) |
| `plugin/harness/scripts/bar-register.py` | **신규** | `bar.jsonl`에 품질 바 항목 append (register/verify/list). `chainlog` 사용 |
| `plugin/harness/hooks/hypothesis-immutability.py` | 수정 | `PROTECTED` 를 set `{hypotheses.jsonl, bar.jsonl}`로 확장 + 파일별 차단 메시지 |
| `plugin/harness/scripts/cycle-init.py` | 수정 | `(cdir/"bar.jsonl").touch()` + cycle-card에 바 등록 안내 한 줄 |
| `plugin/harness/hooks/README.md` | 수정 | hypothesis-immutability 항목에 bar.jsonl 보호 추가 |
| `cycles/<this-cycle>/...` | 생성 | dogfood 사이클 산출물 (ceremony) |

**바 항목 스키마** (`bar.jsonl` 한 줄):
```json
{"id":"B1","criterion":"<품질 요구 텍스트>","stage":"plan|build|test|close|*",
 "measure":"<어떻게 검증되는가 — #007/#008이 읽는 필드>",
 "registered_at":"<iso>","prev_hash":"<64hex>","hash":"<64hex>"}
```
- `stage`: 이 바가 거는 단계. `*`=전 단계.
- `measure`: 강제(Computational) 가능한 형태로 적되, Inferential이면 그 사실을 텍스트로. #007(독립리뷰)·#008(ratchet)이 이 필드를 소비한다.

---

## Chunk 1: 체인 로직 추출 + 가설 스크립트 회귀 무사 (DRY)

> 보안 임계 로직을 한 구현으로 모은다. 기존 `hypotheses.jsonl` 체인이 **그대로 valid** 해야 한다(해시 동일성 보존). 기존 self-test가 회귀 가드.

### Task 1.1 — chainlog.py self-test 작성 (실패 먼저)

- [ ] `plugin/harness/scripts/test-chainlog.sh` 작성:
```bash
#!/usr/bin/env bash
# chainlog 단위 self-test — append 후 verify OK, 변조 후 verify FAIL.
set -u
cd "$(dirname "$0")"
TMP=$(mktemp -d)
python3 - "$TMP/x.jsonl" <<'PY'
import sys; from pathlib import Path
sys.path.insert(0, ".")
import chainlog as c
p = Path(sys.argv[1])
c.append_entry(p, {"id":"A","v":1})
c.append_entry(p, {"id":"B","v":2})
ok, n, err = c.verify_chain(p)
assert ok and n == 2, (ok, n, err)
# 변조: 둘째 줄 v를 손으로 바꿈
lines = p.read_text().splitlines()
import json
e = json.loads(lines[1]); e["v"] = 999
lines[1] = json.dumps(e, ensure_ascii=False)
p.write_text("\n".join(lines) + "\n")
ok2, _, err2 = c.verify_chain(p)
assert not ok2 and "TAMPERED" in (err2 or ""), (ok2, err2)
print("chainlog self-test: PASS")
PY
rc=$?
rm -rf "$TMP"
exit $rc
```
- [ ] 실행해 **실패** 확인 (모듈 없음): `bash plugin/harness/scripts/test-chainlog.sh; echo $?` → non-zero
- [ ] 커밋: `test(harness): chainlog self-test (failing)`

### Task 1.2 — chainlog.py 구현

- [ ] `plugin/harness/scripts/chainlog.py` 작성:
```python
#!/usr/bin/env python3
"""
chainlog.py — Shared tamper-evident hash-chain primitives for append-only cycle records.

hypotheses.jsonl (가설) 와 bar.jsonl (품질 바) 가 같은 체인 모델을 쓴다.
한 곳에서 검증된 구현을 둘이 공유 — 보안 임계 로직의 drift 방지 (DRY).
해시는 (prev_hash + 'hash' 제외 정렬 JSON) 의 SHA-256. 기존 hypotheses.jsonl 과 동일 계산식.
"""
import hashlib
import json
from pathlib import Path

GENESIS = "0" * 64


def compute_hash(entry: dict, prev_hash: str) -> str:
    payload = json.dumps(
        {k: v for k, v in entry.items() if k != "hash"},
        sort_keys=True,
        ensure_ascii=False,
    )
    return hashlib.sha256((prev_hash + payload).encode("utf-8")).hexdigest()


def last_hash(path: Path) -> str:
    if not path.exists():
        return GENESIS
    last = None
    for line in path.read_text(encoding="utf-8").splitlines():
        if line.strip():
            last = json.loads(line)
    return last["hash"] if last else GENESIS


def append_entry(path: Path, entry: dict) -> dict:
    """entry(dict)에 prev_hash·hash를 채워 append하고, 기록된 entry를 반환."""
    prev = last_hash(path)
    entry = dict(entry)
    entry["prev_hash"] = prev
    entry["hash"] = compute_hash(entry, prev)
    with path.open("a", encoding="utf-8") as f:
        f.write(json.dumps(entry, ensure_ascii=False) + "\n")
    return entry


def verify_chain(path: Path):
    """체인 무결성 검증. 반환 (ok: bool, count: int, error: str|None)."""
    if not path.exists():
        return False, 0, f"file not found: {path}"
    prev = GENESIS
    count = 0
    for i, line in enumerate(path.read_text(encoding="utf-8").splitlines(), 1):
        if not line.strip():
            continue
        try:
            entry = json.loads(line)
        except json.JSONDecodeError as e:
            return False, count, f"line {i}: malformed JSON ({e})"
        if entry.get("prev_hash") != prev:
            return False, count, (
                f"line {i} [{entry.get('id')}]: prev_hash mismatch — chain broken"
            )
        expected = compute_hash(entry, prev)
        if entry.get("hash") != expected:
            return False, count, (
                f"line {i} [{entry.get('id')}]: hash mismatch — TAMPERED"
            )
        prev = entry["hash"]
        count += 1
    return True, count, None
```
- [ ] 실행해 **통과** 확인: `bash plugin/harness/scripts/test-chainlog.sh; echo $?` → `chainlog self-test: PASS` + exit 0
- [ ] 커밋: `feat(harness): chainlog shared hash-chain module`

### Task 1.3 — hypothesis-register.py를 chainlog로 리팩터 (동작 불변)

- [ ] `hypothesis-register.py`에서 `import` 줄에 `import chainlog` 추가(같은 디렉터리 — `sys.path`에 스크립트 dir 포함됨; 필요시 `sys.path.insert(0, str(Path(__file__).resolve().parent))`).
- [ ] 로컬 `compute_hash`/`last_hash` 정의 **삭제**하고 `chainlog.compute_hash`/`chainlog.last_hash` 사용.
- [ ] `cmd_register`의 entry 작성을 `chainlog.append_entry(path, {...})`로 교체 (수동 prev_hash/hash 채움 제거). 반환된 entry로 `entry['hash'][:16]` 출력 유지.
- [ ] `cmd_verify`를 `ok, count, err = chainlog.verify_chain(path)`로 교체: ok면 `OK — {count} hypotheses verified, chain intact` + exit 0; 아니면 `FAIL: {err}` + exit 2.
- [ ] **회귀 가드** — 기존 가설 체인이 그대로 valid 한지 확인 (해시 계산식 불변 입증):
```bash
python3 plugin/harness/scripts/hypothesis-register.py verify \
  --cycle 20260531-deploy-kill-check-sensor; echo "rc=$?"
# 기대: "OK — 1 hypotheses verified, chain intact" / rc=0
```
- [ ] **신규 등록 경로** self-test (임시 사이클):
```bash
mkdir -p cycles/_tmp-bartest && python3 plugin/harness/scripts/hypothesis-register.py \
  register --cycle _tmp-bartest --id H1 --hypothesis t --kill-line k --pass-line p \
  && python3 plugin/harness/scripts/hypothesis-register.py verify --cycle _tmp-bartest; echo "rc=$?"
rm -rf cycles/_tmp-bartest
# 기대: REGISTERED ... / OK — 1 ... / rc=0
```
- [ ] 커밋: `refactor(harness): hypothesis-register uses chainlog (behavior identical)`

---

## Chunk 2: bar-register.py — 품질 바 등록

### Task 2.1 — bar-register self-test 작성 (실패 먼저)

- [ ] `plugin/harness/scripts/test-bar-register.sh` 작성:
```bash
#!/usr/bin/env bash
set -u
cd "$(dirname "$0")/../.."   # harness-engineering 루트로
CID=_tmp-barreg
rm -rf "cycles/$CID"; mkdir -p "cycles/$CID"; : > "cycles/$CID/bar.jsonl"
R="python3 plugin/harness/scripts/bar-register.py"
$R register --cycle $CID --id B1 --criterion "gate2 정량 충족" --stage test --measure "self-test N/N" \
  && $R register --cycle $CID --id B2 --criterion "리뷰 지적 0" --stage close --measure "blackbox 0건" \
  && $R verify --cycle $CID \
  && $R list --cycle $CID
rc=$?
rm -rf "cycles/$CID"
[ $rc -eq 0 ] && echo "bar-register self-test: PASS"
exit $rc
```
- [ ] 실행해 **실패** 확인 (스크립트 없음). 커밋: `test(harness): bar-register self-test (failing)`

### Task 2.2 — bar-register.py 구현

- [ ] `plugin/harness/scripts/bar-register.py` 작성 (`hypothesis-register.py` 구조 미러, chainlog 사용):
```python
#!/usr/bin/env python3
"""
bar-register.py — Pre-registration of the *quality bar* with a tamper-evident hash chain.

사이클 시작 시 품질 기준(gate 임계 / DoD / stage별 필수 리뷰)을 bar.jsonl 에 등록한다.
가설(hypothesis-register.py)과 동일한 chainlog — 사후에 바를 *낮추면* verify 가 탐지.
이것이 "지친 에이전트가 중간에 바를 낮추는" 품질 저하 경로의 물리적 방지선 (#006).
#007(독립 리뷰)·#008(ratchet)이 각 항목의 stage·measure 를 소비한다.

Usage:
  bar-register.py register --cycle <id> --id <Bn> \\
      --criterion "..." --stage <plan|build|test|close|*> --measure "..."
  bar-register.py verify --cycle <id>
  bar-register.py list   --cycle <id>
"""
import argparse
import sys
from datetime import datetime, timezone
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
import chainlog  # noqa: E402

CYCLES_DIR = Path("cycles")
STAGES = ("plan", "build", "test", "close", "*")


def bar_file(cycle_id: str) -> Path:
    return CYCLES_DIR / cycle_id / "bar.jsonl"


def cmd_register(args):
    cdir = CYCLES_DIR / args.cycle
    if not cdir.exists():
        print(f"ERROR: cycle directory not found: {cdir}", file=sys.stderr)
        sys.exit(1)
    entry = chainlog.append_entry(
        bar_file(args.cycle),
        {
            "id": args.id,
            "criterion": args.criterion,
            "stage": args.stage,
            "measure": args.measure,
            "registered_at": datetime.now(timezone.utc).isoformat(),
        },
    )
    print(f"REGISTERED bar [{args.id}] stage={args.stage} in cycle {args.cycle}")
    print(f"  criterion: {args.criterion}")
    print(f"  measure:   {args.measure}")
    print(f"  hash: {entry['hash'][:16]}...")
    print()
    print("주의: 이 항목을 *수정*(바 낮추기)하면 verify에서 탐지됨.")
    print("      기준 변경이 필요하면 *새 ID*로 재등록 + ADR.")


def cmd_verify(args):
    ok, count, err = chainlog.verify_chain(bar_file(args.cycle))
    if ok:
        print(f"OK — {count} bar criteria verified, chain intact")
        sys.exit(0)
    print(f"FAIL: {err}")
    sys.exit(2)


def cmd_list(args):
    path = bar_file(args.cycle)
    if not path.exists() or path.stat().st_size == 0:
        print("(no bar criteria registered yet)")
        return
    import json
    for line in path.read_text(encoding="utf-8").splitlines():
        if not line.strip():
            continue
        e = json.loads(line)
        print(f"[{e['id']}] ({e['stage']}) {e['criterion']}")
        print(f"  measure: {e['measure']}")
        print()


def main():
    parser = argparse.ArgumentParser(
        description="Quality-bar pre-registration with hash chain (#006 bar-lock)"
    )
    sub = parser.add_subparsers(dest="cmd", required=True)

    p_reg = sub.add_parser("register", help="Register a quality-bar criterion")
    p_reg.add_argument("--cycle", required=True)
    p_reg.add_argument("--id", required=True, help="Short bar ID, e.g., B1")
    p_reg.add_argument("--criterion", required=True)
    p_reg.add_argument("--stage", required=True, choices=STAGES)
    p_reg.add_argument("--measure", required=True)
    p_reg.set_defaults(func=cmd_register)

    p_ver = sub.add_parser("verify", help="Verify bar chain integrity")
    p_ver.add_argument("--cycle", required=True)
    p_ver.set_defaults(func=cmd_verify)

    p_list = sub.add_parser("list", help="List registered bar criteria")
    p_list.add_argument("--cycle", required=True)
    p_list.set_defaults(func=cmd_list)

    args = parser.parse_args()
    args.func(args)


if __name__ == "__main__":
    main()
```
- [ ] 실행해 **통과** 확인: `bash plugin/harness/scripts/test-bar-register.sh; echo $?` → `bar-register self-test: PASS` / 0
- [ ] 커밋: `feat(harness): bar-register — quality-bar hash chain (#006)`

---

## Chunk 3: 보호 hook 확장 + cycle-init scaffold

### Task 3.1 — immutability hook 차단 self-test 확장 (실패 먼저)

- [ ] `plugin/harness/hooks/README.md` Self-test 절 아래로 bar.jsonl 케이스를 *지금 실행*해 **현재는 통과(exit 0)** 임을 확인 — 즉 미보호 상태 입증:
```bash
echo '{"tool_name":"Edit","tool_input":{"file_path":"x/bar.jsonl"}}' \
  | python3 plugin/harness/hooks/hypothesis-immutability.py; echo "rc=$?"
# 현재 기대: rc=0 (아직 미보호 — 이게 우리가 고칠 갭)
```
- [ ] (이 단계는 가드 기록용 — 커밋 불필요, 다음 task에서 rc=2로 뒤집힘)

### Task 3.2 — PROTECTED 확장

- [ ] `plugin/harness/hooks/hypothesis-immutability.py` 수정:
  - 모듈 docstring 첫 줄을 "등록된 *append-only 체인 파일*(hypotheses.jsonl, bar.jsonl)" 로 일반화.
  - `PROTECTED = "hypotheses.jsonl"` → 매핑으로 교체:
```python
# 보호 대상 append-only 체인 → 정당 등록 스크립트 안내
PROTECTED = {
    "hypotheses.jsonl": "hypothesis-register.py",
    "bar.jsonl": "bar-register.py",
}
```
  - `main()`의 매칭부를 수정:
```python
    for p in target_paths(tool_input):
        name = Path(p).name
        if name in PROTECTED:
            register = PROTECTED[name]
            sys.stderr.write(
                f"BLOCKED: {name} 직접 편집 금지 (AP-06 Gate fudging / #006 바 낮추기 방지).\n"
                f"  이 파일은 tamper-evident hash chain 으로 보호된다.\n"
                f"  항목 추가는: python3 ${{CLAUDE_PLUGIN_ROOT}}/scripts/{register} register ...\n"
                f"  기존 항목 변경이 필요하면 *새 ID* 로 재등록 + ADR.\n"
            )
            sys.exit(2)
    sys.exit(0)
```
- [ ] self-test (양쪽 차단 + 무관 파일 통과):
```bash
H=plugin/harness/hooks/hypothesis-immutability.py
echo '{"tool_name":"Edit","tool_input":{"file_path":"x/hypotheses.jsonl"}}' | python3 $H; echo "hyp rc=$?"   # 2
echo '{"tool_name":"Edit","tool_input":{"file_path":"x/bar.jsonl"}}'        | python3 $H; echo "bar rc=$?"   # 2
echo '{"tool_name":"Edit","tool_input":{"file_path":"x/cycle-card.md"}}'    | python3 $H; echo "ok  rc=$?"   # 0
echo 'not json'                                                              | python3 $H; echo "fail-open rc=$?" # 0
```
- [ ] 기대: hyp=2, bar=2, ok=0, fail-open=0. 커밋: `feat(harness): immutability hook also protects bar.jsonl (#006)`

### Task 3.3 — cycle-init이 bar.jsonl scaffold

- [ ] `plugin/harness/scripts/cycle-init.py` 수정:
  - 다른 `.touch()` 들 옆에 `(cdir / "bar.jsonl").touch()` 추가 (hypotheses.jsonl·blackbox.jsonl 근처, 라인 288 인근).
  - `CYCLE_CARD` 의 "## 관련 문서" 목록에 한 줄 추가: `- Quality bar (잠금): ./bar.jsonl  → bar-register.py 로 등록 (#006)`.
  - `main()` 끝 "다음 단계:" 출력에 추가: `print(f"  5. scripts/bar-register.py register --cycle {cid} --id B1 --stage test --criterion ... --measure ... (품질 바 잠금)")`.
- [ ] self-test (임시 사이클 생성 → bar.jsonl 존재 + 비어있음 → 정리):
```bash
cd "$(git rev-parse --show-toplevel)/_draft/harness-engineering"
# active 사이클 있으면 임시로 비켜야 함: 별도 임시 cycles 루트에서 테스트
TMPROOT=$(mktemp -d); cp plugin/harness/scripts/cycle-init.py "$TMPROOT/"
( cd "$TMPROOT" && python3 cycle-init.py bartest --type dev-tool >/dev/null && \
  test -f cycles/*-bartest/bar.jsonl && echo "bar.jsonl scaffolded: PASS" )
rm -rf "$TMPROOT"
```
- [ ] 기대: `bar.jsonl scaffolded: PASS`. 커밋: `feat(harness): cycle-init scaffolds bar.jsonl (#006)`

### Task 3.4 — hooks/README.md 갱신

- [ ] `plugin/harness/hooks/README.md` 의 `hypothesis-immutability.py` 표에서 "막는 것" 행을 "`hypotheses.jsonl`·`bar.jsonl` 직접 편집 차단 (AP-06 Gate fudging + #006 바 낮추기)" 로 갱신, Self-test 절에 bar.jsonl 차단 케이스 한 줄 추가.
- [ ] 커밋: `docs(harness): README — hook now guards bar.jsonl`

---

## Chunk 4: Dogfood 사이클 ceremony (자기적용)

> 이 변경 자체를 하네스 규칙대로 한 사이클로 돌린다. **Chunk 1~3 빌드 전에** 4.1~4.2(게이트+가설+바 등록)를 먼저 하는 것이 정석이나, 실행 편의상 빌드와 병행 가능. 핵심은 **종료 전 4.3~4.5 완수**.

### Task 4.1 — pre-cycle 게이트 + 사이클 생성

- [ ] WIP 확인: `python3 plugin/harness/scripts/cycle-init.py --check-wip` → active 없음 확인 (있으면 먼저 종료).
- [ ] 생성: `python3 plugin/harness/scripts/cycle-init.py "bar lock" --type dev-tool`
- [ ] `cycle-card.md` 채우기: cycle type=Dev-tool, appetite_sessions=2 (체인 추출+신규 스크립트+hook+init+ceremony — #005보다 큼, 정직하게 2), kill 기준 확인.

### Task 4.2 — 가설 + 바 등록 (잠금)

- [ ] 가설 등록:
```bash
CID=<this-cycle-id>
python3 plugin/harness/scripts/hypothesis-register.py register --cycle $CID --id H1 \
  --hypothesis "bar-register.py가 품질 바를 hash chain으로 등록하고, immutability hook이 bar.jsonl 직접 편집을 차단(exit2)하며, 정당 경로(bar-register append)는 통과, verify가 변조를 탐지한다. chainlog 추출 후 기존 hypotheses 체인은 회귀 없이 valid." \
  --kill-line "bar.jsonl 직접 Edit를 통과시킴 OR 정당 append를 차단함 OR 기존 hypotheses verify가 깨짐 → 기각" \
  --pass-line "self-test: chainlog PASS, bar-register PASS, hook(hyp=2/bar=2/ok=0/fail-open=0), 기존 hypotheses verify OK"
```
- [ ] **바 등록(이 사이클의 품질 바 — dogfood)**:
```bash
B="python3 plugin/harness/scripts/bar-register.py register --cycle $CID"
$B --id B1 --stage test  --criterion "전 self-test 통과" --measure "chainlog/bar-register/hook self-test 전부 PASS"
$B --id B2 --stage test  --criterion "기존 가설 체인 회귀 0" --measure "hypothesis-register verify OK (deploy-kill-check 사이클)"
$B --id B3 --stage close --criterion "override/skip 0건" --measure "blackbox.jsonl 비어있음"
```
- [ ] `python3 plugin/harness/scripts/bar-register.py verify --cycle $CID` → chain intact 확인.

### Task 4.3 — 통합 self-test 일괄 실행 (바 B1 충족 입증)

- [ ] 네 self-test 순차 실행, 모두 PASS 캡처:
```bash
bash plugin/harness/scripts/test-chainlog.sh
bash plugin/harness/scripts/test-bar-register.sh
# hook 4-case (Task 3.2) + 기존 hypotheses verify (Task 1.3 회귀 가드)
```
- [ ] `metrics.json`: gate2_status에 self-test 결과 기록, kill_check 갱신.

### Task 4.4 — retro + black box 대면

- [ ] `cycles/$CID/retro.md` 작성: 가설 결과(H1 지지/기각 근거=self-test), 배운 것(체인 추출로 DRY+drift 방지, "측정 가능한 바만 강제" 가 #007/#008의 measure 필드로 이어짐), 살림/의심/버림.
- [ ] **black box 대면**: `blackbox.jsonl` 확인 — override/skip 0건이어야 B3 충족. 어긴 룰 있으면 정직 기록.
- [ ] `findings.md`: 빌드 중 발견한 갭 기록 (예: hook 이름이 이제 'hypothesis'만 가리키지 않음 → 향후 rename 후보 / chainlog import 경로 주의).

### Task 4.5 — 명시적 종료 (SD-07) + 문서 갱신

- [ ] `devils-advocate.md` Resolution log에 #006 항목 추가 (시간 순서 유지 — #005 다음).
- [ ] `TODO.md` 갱신: Now에서 #006 제거, Done에 추가, 다음 Now = #007(독립 리뷰 게이트).
- [ ] active 심링크 제거로 사이클 종료: `rm cycles/active` (SD-07 — 종료는 명시적).
- [ ] 최종 커밋: `feat(harness): bar-lock (#006) — quality bar hash chain + ceremony`

---

## 정직한 한계 (이 plan이 *하지 않는* 것)

- 바의 *내용 적절성*은 강제하지 않는다 — 사람/Claude가 시작 시 정한다. 강제하는 건 *"한 번 정하면 못 낮춘다"* 뿐.
- `measure` 필드는 지금 **소비자가 없다**. #007(독립 리뷰)·#008(ratchet)이 이 필드를 읽어야 비로소 "바 충족 여부"가 close 게이트로 강제된다. #006은 *바를 잠그는 것까지*. 이 plan만으로 품질 저하가 막히는 게 아니라 — **#006+#007+#008 합쳐야** 막힌다. (설계 단계에서 합의된 분해)
- hook 파일명은 `hypothesis-immutability.py` 유지(이미 hooks.json에 wired — rename은 별도 위험). 이제 의미상 더 넓음 → findings에 rename 후보로 기록.

---

## Execution Handoff

Plan complete and saved to `_draft/harness-engineering/plans/2026-05-31-bar-lock.md`.

서브에이전트 사용 가능 환경이므로 표준은 **superpowers:subagent-driven-development**(task당 fresh subagent + 2단계 리뷰)다 — 그러나 이 저장소의 "사용자 요청 시에만 에이전트 스폰" 지침에 따라, 실제 실행 방식(현 세션 직접 실행 vs subagent 분산)은 **사용자 승인 후 결정**한다. Ready to execute?
