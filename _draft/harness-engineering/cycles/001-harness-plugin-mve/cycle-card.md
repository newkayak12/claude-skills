# Cycle Card — #001 Harness Plugin MVE

> 1장 요약. 이 사이클이 *무엇을 검증하는지*와 *언제 죽이는지*를 한눈에.

## 메타

| 항목 | 값 |
|---|---|
| 제목 | Harness Plugin MVE — 하네스를 *실행 가능한 설치형 스킬*로 |
| 시작일 | 2026-05-31 |
| 시간 예산 (appetite) | **1 작업 세션** (Shape Up appetite식 — fixed time, variable scope) |
| 사이클 타입 | **dev-tool / self-user** (제품 사이클 아님 — dogfood) |
| Persona 가설 | 엔지니어 출신 1인 개발자 = *나 자신* (n=1 self-user) |

## 문제 진술 (해결책 아님)

> 솔로 dev(나)가 제품 사이클을 끝까지 못 끌고 간다 — 검증을 건너뛰고, WIP를 못 지키고, 출시를 미룬다. 현재 대안(머릿속 규율 + 산발적 메모)은 표류한다 (`devils-advocate.md CV-1`).

## 핵심 가설 (≤3, falsifiable)

- **H1** — *하네스를 `harness:cycle` 스킬로 설치 가능하게 만들면, 다음 실제 프로젝트에서 pre-cycle 게이트를 (건너뛰지 않고) 실제로 실행한다.*
  - 반증 조건: MVE 설치 후 다음 프로젝트에서 게이트를 *호출하지 않으면* H1 기각.
- **H2** — *pre-cycle 게이트(09)를 대화형 스킬로 실행하면, 마크다운을 직접 읽는 것보다 빠짐없이 통과한다.*
  - 반증 조건: 스킬 실행이 *수동 체크리스트보다 항목 누락이 많거나* 시간이 더 들면 기각.

## MVE 범위 (이번 사이클이 *만드는 것*)

가장 얇은 설치 가능 슬라이스 1개:

1. `harness` 플러그인 골격 (`.claude-plugin/` + marketplace 등록 가능 구조)
2. `harness:cycle` 스킬 1개 — pre-cycle 게이트(09)를 *대화형*으로 실행 + 산출물 3종 scaffold
3. `cycle-init.py`를 스킬에서 호출되도록 연결

**범위 밖 (이번 사이클 아님)**: interactive user-rule(L1) 설정, hook 실구현, rules-as-code, 토큰 최적화, 13 §7 전체.

## 성공 기준 (Gate 통과 수치)

dev-tool 타입이라 `08`의 제품 Gate를 *적응*:

| Gate | 기준 (적응됨) |
|---|---|
| Gate 1 (제품→*도구 유용성*) | 게이트 스킬이 *이 사이클 자신*에 한 번 end-to-end 실행되어 산출물 3종을 만든다 (self-test) |
| Gate 2 (기술) | `harness:cycle` 스킬이 실제 설치 구조에서 로드되고, `cycle-init.py`가 에러 없이 폴더를 scaffold |
| DoD | 다음 실제 프로젝트에서 *한 번* 호출 가능한 상태 (H1 측정 준비 완료) |

## Kill 기준 (사전 정의 — §07 §7.5)

| 종류 | 임계값 |
|---|---|
| **Hard** | 재진입 3회 / 시간 200% (= 2 세션 초과) / "설치 가능 골격"이 1세션 내 안 나옴 |
| **Soft** | 시간 150% (= 1.5 세션) — 범위 재검토 신호 |
| **즉시 kill** | MVE가 *기존 마크다운 정리*로 변질되면 (= 설치 가능한 게 안 나옴) → AP-05 ceremony |

## 이전 사이클 인계 (살림/의심/버림)

첫 사이클이라 인계 없음. 단 *컨셉 draft에서* 가져오는 것:
- **살림**: 09 게이트 체크리스트, cycle-init.py, 12/13의 layer·tier 개념
- **의심**: 16개 hook spec (실제 필요 미검증), 25개 AP (재인식 가능성 `CA-7`)
- **버림**: 없음 (아직 실전 데이터 없음)

## 현재 단계

`pre-cycle ✅ → 기획 ✅ → 개발 ✅ → 테스트 ✅ → retro ✅` — **CLOSED** (2026-05-31).
결과: `retro.md` 참조. H1은 다음 실제 프로젝트에서 측정 대기.
